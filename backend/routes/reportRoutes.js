import express from 'express';import { dashboard,salesReport,purchasesReport,lowStockReport,profitReport } from '../controllers/reportController.js';import { protect } from '../middleware/authMiddleware.js';const router=express.Router();router.use(protect);router.get('/dashboard',dashboard);router.get('/sales',salesReport);router.get('/purchases',purchasesReport);router.get('/low-stock',lowStockReport);router.get('/profit',profitReport);export default router;
